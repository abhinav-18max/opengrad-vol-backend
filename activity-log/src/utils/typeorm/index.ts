import { DailyLog } from '../../attendence/entities/daily-log.entity';
import { Log } from '../../attendence/entities/log.entity';

const entities = [DailyLog, Log];
export default entities;

export { DailyLog, Log };
